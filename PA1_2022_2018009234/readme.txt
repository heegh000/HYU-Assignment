readme


In my Python environment, astype(np.float) causes warnnings.
The Python IDLE recommended you should change that to astype(np.float64)
But I don't know the warnnings are caused in test environment 
So, I will upload both versions containing astype(np.float) or astype(np.float64)
Default version of raytracer.py is consits of astype(np.float64)
If test environment requires astype(np.float), please run the raytracer_astype(float)
I'm sorry for borthering you. Thank you for reading 

제 파이썬 환경에서 astype(np.float)이 warnning을 발생시킵니다.
파이썬 IDLE는 astype(np.float64)로 바꿀 것을 권장하는데, 테스트 환경이 어떠한지 몰라서
astype(np.float)으로 된 버전과, astype(np.float64)로 된 버전 두개를 모두 업로드 하겠습니다.
기본 raytracer는 astype(npfloat64)로 코딩되어 있습니다.
만약 테스트 환경에서 astype(np.float)가 필요하다면 "raytracer_astype(float)"를 실행시켜주시면 감사하겠습니다
번거롭게해서 죄송합니다. 감사합니다.